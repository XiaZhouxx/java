<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.xz.mapper.PartitionDao">
    <insert id="add" parameterType="cn.xz.bean.Partition">
      insert into t_gared_partition values(#{province},#{levCode},#{subCode},
        #{yeaCode},#{grade},#{userNum},#{userTotal},null);
    </insert>
</mapper>